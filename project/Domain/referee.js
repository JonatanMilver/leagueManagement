// inherits systemRole

let referee = {
    ualification: string,
    isHeadReferee: Boolean,
    games: Array,
    seasons: Array
};


async function setGameScore(gameID, homeTeamGoals, awayTeamGoals){
}

async function addEvent(gameID, time, gameMinute, eventType, description){
}

async function editEvent(gameID, time, gameMinute, eventType, description){
}